name: libgen
description: Dart wrapper for the Library Genesis API
version: 0.0.1
homepage: https://github.com/hobroker/libgen.dart

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  freezed_annotation: ^0.12.0
  json_serializable: ^3.5.0

dev_dependencies:
  build_runner: ^1.10.1
  freezed: ^0.12.1
  pedantic: ^1.9.0
  test: ^1.15.3
